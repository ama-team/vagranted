dependencies:
  beta: fs://../beta
context:
  current_set: alpha
  alpha: 1
templates:
  - pattern: '*.twig'
    name: '{{ basename }}.rendered'
    exclusions:
      - current-set.twig
assets:
  - '*.asset'
